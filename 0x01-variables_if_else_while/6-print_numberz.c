#include <stdio.h>
/**
 * main-this function displays numbers of the base 10 range
 * Return: 0
**/
int main(void)
{
	int x;

	for (x = 0; x < 10; x++);

	putchar(x + '0');
	putchar('\n');
	return (0);
}
